- `<dj-call function="quip.has_vote" request="request" as="has_vote" />`

- publish package from GitHub Action instead of from local
- slots
  - more tests around slots
  - default slots, not just named slots
  - enable by default
  - how work with `with`?
- test includes with `with`

- think about code control template tags, i.e. if statements, for loops (they don't feel like they belong, but 🤷)
- think about styles per non-shadow component
- think about JS per component
- typechecker
